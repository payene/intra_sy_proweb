{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeDemande</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeDemande.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typeDemande.libelle }}</td>
            </tr>
            <tr>
                <th>Del</th>
                <td>{{ typeDemande.del }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typedemande_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typedemande_edit', { 'id': typeDemande.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

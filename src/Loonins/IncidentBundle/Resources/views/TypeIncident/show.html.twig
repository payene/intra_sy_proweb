{% extends 'LooninsIncidentBundle::layout.html.twig' %}

{% block body -%}
    <h1>TypeIncident</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity.type }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typeincident') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('typeincident_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

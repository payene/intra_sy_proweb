{% extends 'LooninsGrhBundle::layout.html.twig' %}

{% block body -%}
<h1>GrhDepartement</h1>

<table class="table table-striped record_properties">
    <tbody>
        <tr>
            <th>Departement</th>
            <td>{{ entity.departement }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        {% include "LooninsGrhBundle:GrhDepartement:listeDepartement.html.twig" %}
    </li>
    <li>
        <a href="{{ path('grhdepartement_edit', { 'id': entity.id }) }}">
            <button class="btn btn-warning"> <i class="icon icon-pencil"></i> Editer </button>
        </a>
    </li>
    <li>
{#{{ form(delete_form) }}#}
{#         <form action="{{ path('grhdepartement_delete',{'id': entity.id})  }}" method="post">
            <div>
{#                    {# Génération de l'input. #}
             {#   {{ form_widget(delete_form.submit,{ 'attr': {'class':'btn btn-danger'} }) }} 
            </div>
        </form>#}
        {% include "LooninsGrhBundle:GrhDepartement:deleteDepartement.html.twig" %}
    </li>
</ul>
{% endblock %}

{% extends 'LooninsGrhBundle::layout.html.twig' %}

{% block body -%}
    <h1> Type de Fiche Employ&eacute;</h1>

    <table class="records_list table table-hover">
        <thead>
            <tr>
{#                <th>Id</th>#}
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
{#                <td><a href="{{ path('grhtypefiche_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>#}
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                   {# <li>
                        <a href="{{ path('grhtypefiche_show', { 'id': entity.id }) }}">Details </a>
                    </li>#}
                    <li>
                        <a href="{{ path('grhtypefiche_edit', { 'id': entity.id }) }}">Modifier</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

       
            <a class="btn btn-info" href="{{ path('grhtypefiche_new') }}">
                Ajouter un nouveau type de fiche
            </a>
        
    {% endblock %}

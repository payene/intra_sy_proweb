{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Matrimonial list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Sitmat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('matrimonial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sitMat }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('matrimonial_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('matrimonial_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('matrimonial_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

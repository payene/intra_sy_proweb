{% extends 'LooninsGrhBundle::layout.html.twig' %}

{% block body -%}
    <h3> Fiche salari&eacute; de
        <i> {{ entity.employe.nom  }}  {{ entity.employe.prenoms  }} </i>
        <br/>
        <a href="{{ path('grhfichesalarie_new')}}" class="btn btn-primary"> <i class="icon-white icon-plus"></i> Ajouter un nouvel &eacute;l&eacute;ment </a>
    </h3>

    <div class="panel alert alert-info">
        Le {{  entity.date|date("d-m-Y a H:i:s") }} |<i> {{ entity.titre }} </i> | <b>{{ entity.type.type }}</b>
        <p> {{ entity.contenu }} </p>
        <a href="{{ path('grhfichesalarie_edit', {'id':entity.id}) }}" class="btn btn-warning" ><i class="icon icon-edit"></i> </a>

    </div>

    <ul class="record_actions">
        <li>
            <a href="{{ path('grhemployes_fiche',{'id':entity.employe.id}) }}">
                Retourner sur la fiche salari&eacute;
            </a>
        </li>
    </ul>

    {#<table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ entity.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ entity.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>#}

    <ul class="record_actions">
       {# <li>
            <a href="{{ path('grhfichesalarie') }}">
                Back to the list
            </a>
        </li>#}
        {#<li>
            <a href="{{ path('grhfichesalarie_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>#}
        {#        <li>{{ form(delete_form) }}</li>#}
    </ul>

{% endblock %}